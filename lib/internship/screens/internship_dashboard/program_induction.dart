import 'package:flutter/material.dart';

class ProgramInductionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Program Induction'),
      ),
      body: Center(
        child: Text('Details about the Program Induction.'),
      ),
    );
  }
}
